const createMachine = require("./createMachine");
const getMachine = require("./getMachine");
const increaseMachineCounter = require("./increaseMachineCounter");

module.exports = {
  getMachine,
  createMachine,
  increaseMachineCounter,
};
